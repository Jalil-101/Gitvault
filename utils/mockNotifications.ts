import type { Notification } from "@/types/notifications";

export const generateMockNotifications = (): Notification [] => {
  // Move your mock generation logic here
  // This keeps test data separate from business logic
};
